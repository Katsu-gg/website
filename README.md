## Getting Started
1. Create a new file named: `config.json` and add this values:
```json
{
    "client": {
        "id": "application-id",
        "secret": "application-secret"
    }
}
```

2. Run `npm run dev`